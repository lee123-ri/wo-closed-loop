# 工单管理平台 — 总纲

> 新能源电站运维工单管理面板的生产化版本，基于阿里云 ACK 部署。

| 字段 | 值 |
|------|-----|
| 定位 | 工单管理平台（LLM 版） |
| 当前状态 | 待上线（上线前改造完成并本机回归全绿；剩环境事项：Docker 镜像实测 / 钉钉事件订阅 / ACK 部署）；**本机已对局域网开放试用（见下）**；2026-09-03 追加待本机回归：① 项目编码统一/用户搜索/操作日志审计 ② 登录收编为钉钉 OAuth 且仅管理员放行（需钉钉后台登记回调域名）；**当日恢复本机开发登录**：钉钉回调域名未落地，`DEV_LOGIN_ENABLED=true` + 前端 `frontend/.env.development`（VITE_DEV_LOGIN=true，不入库），重启前后端后登录页出「开发环境登录」，李沛东=admin 可进；**09-03 晚测试闸门两处修复（代码已落，待本机全量回归）**：AuditLog 模型补声明 `created_at` 列（0001 迁移物理表本就有该列，纯模型漏声明，无需新迁移；此前 /audit-logs 序列化 AttributeError）+ conftest 新增登录开关归位夹具（本机 .env 应急开关 `DEV_LOGIN_ENABLED=true` 不再泄漏进测试，test_dev_login_disabled_by_default 本机恒红问题根除）；另澄清：姓名登录 403 非回归，是 09-03 收编预期——门禁跑时盘上还是旧版测试（期望 200），其后已替换为重写版（姓名登录默认 403），该失败项现已不存在；**09-03 主列表已闭环默认归档**：`GET /work-orders` 默认排除 closed（`include_closed=true` 或显式 `status=closed` 可查），前端主列表状态下拉去掉「已闭环」、看板「已闭环」卡改跳闭环记录页 `/closed`，新增 5 回归用例待本机回归；**09-03 晚工单详情页三项修复（代码已落，待本机验收）**：① 审批流转改三步口径展示（待派发→执行中→已闭环；审批中/已派发/待验收等旁支状态归「执行中」、驳回归「待派发」标 warn；原 liveOA 折叠分支一并撤换）② 「🔄 同步 OA」手动按钮恢复常驻（原先无真实 OA 时隐藏；未发起 OA 时点击改弹警示提示，不假报成功）③ 详情页新增 15s 轮询同步（有真实 OA 且未终态 → 先调后端 `/dingtalk/oa/sync` 拉钉钉最新状态/附件，再静默刷新工单/日志/附件，页面显示「轮询同步已开启」，离开页面自动清理）；沙盒 vue-tsc -b 全绿 + flowProgress 逻辑桩 8 断言 PASS，本机 `npm run build` + UI 验收见 `交接终端prompt-0903详情同步.md`；**09-03 drive 定稿重导修码版（代码已落，待本机跑）**：v2 实测暴露 3 处逻辑 bug+1 下载隐患已修——①关键词回退整体删除（只扫定稿夹，失败明示停止不静默回退；此回退是 44 条初稿污染的根因）②`_resolve_doc_folder` 带 `--space-id` 且从 result 层取 uuid ③`_norm_node` 接受无 extension 的 alidocs file 节点 ④下载移除不存在的 `doc +download`，uuid 经 `+info` 换候选 id 逐个 `drive download`；库内既有 `source='plan'` 工单全部来自草稿回退（44+约15 条），重导前先盘点清场（见 `交接终端prompt-0903定稿重导v3.md`）；**09-03 晚项目编码杂码排查**：页面编码列仍现历史杂码，根因=`f001_normalize_project_codes` 未在本机库执行（alembic_version 停 e001；lifespan 只 create_all 不跑 alembic），代码三入口已合规无需改码，沙盒对 round3 备份预演 893 杂码行→PRJ-0370~1262 零碰撞零重复，已发 `交接终端prompt-项目编码归一-20260903.md` 待终端执行后验收；**09-10 前端列表状态跨页保留（keep-alive 扩展）**：左侧菜单进入工单列表/数据池/闭环记录/项目管理/用户管理后，去详情或切其他页再返回，筛选与分页不再重置回初始；**09-10 alert（监视告警）五阶段闭环改造**：8 异常指标大类+每类默认责任人进规则配置、异常指标每日同步轮询、五阶段流转（分析确认→派发→2/11 跟踪→复核→闭环）+复用/合并+发生记录（见版本历史）；**09-14 管理员工单操作**：详情页管理员「编辑基本信息」+「删除起不了状态的废单」，后端 `PATCH /work-orders/{id}/basic` / `DELETE /work-orders/{id}` 均 require_admin（见版本历史）；**09-15 项目筛选栏改列表内项目**：工单列表/闭环记录的项目下拉只列列表内出现过的项目，不再列「无工单」项目（见版本历史）；**09-16 计划非EAM 改初稿夹轮询**：年度运营计划非EAM 工单除定稿夹外，新增钉盘「初稿」文件夹自动轮询（33 个项目子文件夹、只导「工单版」，source=plan 自动进工单列表，见版本历史）；**09-17 数据范围角色可配置化（新增事业部PMO + 自身相关/区域/全部三档范围后台可配）**：见版本历史 |
| 完成度 | 前端 Vue3+TDesign · 后端 FastAPI+PG；鉴权收编、姓名登录、数据源容器化（钉盘+AI表格）、迁移链修复、39 用例全绿（09-02 口径）；09-03 追加编码治理/用户搜索/操作日志审计/登录收编/drive 定稿导入/主列表闭环归档，套件扩至 85 用例（待本机全量回归）；09-10 追加热链：Excel xlsx 导入、告警列进程内兜底、列表状态跨页保留、**外部建单 API（+16 用例）；**09-10 alert 五阶段闭环**（8 异常指标大类+每类默认责任人、异常指标每日同步、五阶段流转+2/11 进度+复用/合并+发生记录，+28 用例）；套件 133 用例本机全量回归绿（0 failed）** |
| 当前版本 | v0.6.0（交付包 `wo-closed-loop-v0.6.0.tar.gz`，SHA256 `89e9a4fc…f24387e9`，打包于 2026-09-02） |
| 负责人 | 李沛东 |

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.6.0 未交付增量 | 2026-09-17 | **工单来源/类型/异常指标大类三合一 → 单一「工单类型」10 类（代码已落，本机回归绿 225 passed / 0 failed）**：`source_code` 承载唯一类型——运营计划(plan) + 8 类异常(power_gen/curtailment/dual_rule/reliability/info_quality/contract/cost/satisfaction，走 alert 五阶段) + 关键会议(meeting) + 后台极简新增(只输名字，自动 `custom_N`，走计划流)。`metric_type` 保留做异常细分与五阶段判定信号，`type_id`/SOP 知识库本轮不迁；五阶段判定全改 `metric_type is not None`，`WorkOrderOut` 增 `is_measure` 区分主单/措施。迁移 `j001`（存量回填 source_code + 幂等灌 10 类 work_order_type 配置，粗映射：残留 manual/external/measure/alert→meeting）；配置合并成 `config_definitions.category=work_order_type`（flow + 默认审批人 + 仅异常类默认责任人），API `GET/POST/PATCH /config/work-order-types`（极简新增）+ SOP CRUD 挪 `/config/sop-types`；前端建单「来源+类型」下拉合一、列表/闭环筛选「来源」→「工单类型」、ConfigPage「工单模板+告警分派」两卡并一张；`oa_required_missing` 的「工单类型」判定改查 source_code。测试断言同步收口（test_api/test_auth_enforcement 的 `/config/sources`、test_alert_flow/test_alert_merge 的 measure、test_agent_import 的 alert 等）。钉钉后台 OA 模板「工单类型」下拉已同步为 10 新类（用户 2026-09-17 完成） |
| v0.6.0 未交付增量 | 2026-09-17 | **数据范围角色可配置化：新增「事业部PMO」角色 + 范围改后台可配（代码已落，待本机回归）**：原数据范围硬编码在 `scope.py`（admin→全部 / 区域PMO→区域+本人 / 其他→本人），事业部PMO（`role_assignments.pmo`）在数据范围上等同普通人，且改口径要动代码。改动 ① 新增 `role_data_scopes` 表（role_code/role_name/scopes JSONB/is_locked/sort_order，alembic `k001`），4 个数据范围角色：admin(系统管理员,锁死 all)、division_pmo(事业部PMO,默认 all 可改)、region_pmo(区域PMO,默认 self+region 可改)、member(普通成员,默认 self 可改)；seed.py 增 `seed_role_scopes`。② `scope.py` 重写：`resolve_data_role` 把人归角色（admin→role_assignments 的 division_head/pmo→region_pmos→member），`apply_scope_to_query` 读 `role_data_scopes` 勾选落 OR 并集（self=本人责任/审批、region=工单大区∈负责区域、all=不限；admin 锁死全部不读配置；勾选空集=显式无范围不误放量）；`apply_personal_scope_to_query`（我的工单）不动。③ 后端 `GET/PUT /config/role-scopes`（PUT require_admin、admin 行 400 锁定、校验 scopes⊆self/region/all，落审计 update_role_scope）。④ 前端 ConfigPage 增「🔐 数据权限」卡片（每角色三 checkbox 多选、admin 置灰不可改）。新增 `tests/test_role_data_scope.py` 9 用例（admin 锁定/事业部PMO 全量/区域PMO 去 self/成员授 all/空集无范围/校验值/未知角色 404）。沙盒 py_compile 全绿；vue-tsc 本改动无新增报错（仓库存在并发重构遗留的 `getSources/getAnomalyCategories/updateAnomalyCategory` 未迁移报错，非本轮引入）；本机回归：定向 19 passed（9 本新增 + 10 scope）、全量 210 passed（15 failed 归因三合一 j001 未迁移测试，本轮零引入）；**同日收口三合一残留**（清 test_api/test_auth_enforcement 的 `/config/sources` 旧断言→work-order-types + 前端 ConfigPage 弹窗去「来源」选项 + ClosedRecords「来源」列→「工单类型」），本机回归绿：**225 passed / 0 failed**（前端 vue-tsc -b 构建零报错） |
| v0.6.0 未交付增量 | 2026-09-16 | **可观测性三件套：文件日志 + 错误告警 + 主动巡检（代码已落，待本机回归）**：解决「工单提交失败没日志可查、只能靠别人转述」的排查盲区。① 结构化文件日志 `core/logging.py`：标准 logging + TimedRotatingFileHandler，写 `backend/logs/app.log`（全量）+ `error.log`（仅 ERROR 起）、按天滚动保留 `LOG_RETENTION_DAYS` 天、`LOG_TO_STDERR` 可选同打终端、`get_logger/setup_logging` 幂等；`main.py` 启动即 `setup_logging`，加 `@app.exception_handler(Exception)` 兜底未捕获异常→traceback 落 error.log + 单发告警（业务自抛 422/409 走 FastAPI 内置 handler 不误报）。② 错误告警 `services/alert_service.py`：钉钉工作通知单发给 `ALERT_NOTIFY_USERID`（空回落 `DINGTALK_FALLBACK_USERID`；.env 已配 李沛东 userId），`ALERT_DEBOUNCE_SECONDS` 防抖去重，告警带 summary/detail/「怎么修」；触发点=未捕获异常 + `workorders._launch_oa` 已配置钉钉却未生成审批实例（820003 类）→`_alert_oa_fail`，派发被拦加 `log.warning`。③ 主动巡检 `services/health_sweep.py`：每 `SWEEP_INTERVAL` 秒扫「pending 却缺 OA 必填字段的工单（派发会被 422 拦）」+「异常指标/计划同步超 3×interval 没成功」（读 `sync_poller.sync_health()`，sync_poller 改在 poll 循环更新 last_ok），发现问题汇总一条告警并附修复动作；本地 dev 由 lifespan 进程内轮询、生产由 Celery beat `app.tasks.sweep`(600s) 跑。④ 配置：config.py/.env/.env.example 加 `LOG_DIR/LOG_RETENTION_DAYS/LOG_TO_STDERR`、`ALERT_ENABLED/ALERT_NOTIFY_USERID/ALERT_DEBOUNCE_SECONDS`、`SWEEP_ENABLED/SWEEP_INTERVAL`；新增 tests/test_logging_alert_sweep.py 8 用例。沙盒 py_compile 全绿，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-16 | **年度运营计划「初稿」文件夹轮询同步非EAM 计划工单（代码已落，待本机回归）**：原「只扫钉盘定稿夹、初稿当污染排除」口径反转——定稿夹仍扫，另把钉盘 alidocs「初稿」文件夹（`Agent运营计划初稿（待判定会及确认工单）`，nodeId=`b9Y4gmKWrPqYGdg9i4y44M2AJGXn6lpz`、与定稿夹同 space 26687726819、33 个项目子文件夹）并入轮询源。改动 ① `drive_workorder_import.py`：新增 `_draft_folder_id()`（走 get_settings 读 `drive_draft_plan_folder_id`）+ `_walk_folder` 只收文件名含「工单版」的文件（初稿每项目有视觉/汇报/文档/工单版 4 类，只导工单版）+ `find_workorder_versions` 扫「定稿+初稿」两夹并集（按节点 id 去重、单夹失败不阻断另一夹、仍不回退关键词）；② `sync_poller.py` 增 `run_plan_sync`/`poll_plan_loop`（每 `plan_sync_interval` 秒跑 `import_drive_workorder_versions`，默认 3600s），`start/stop` 双任务；③ `config.py` 增 `drive_draft_plan_folder_id`+`plan_sync_interval`，`main.py` lifespan 传双间隔；④ `tasks.py` 增 `sync_plan_draft`、`celery_app.py` beat 增 `sync-plan-draft`(3600s)。工单落 `source=plan`、status=pending，自动进工单列表；项目+标题去重、补空不补错不变。新增 tests/test_drive_import_parse.py 2 用例（工单版过滤/双夹扫描去重）。沙盒 py_compile + 纯逻辑桩 PASS，本机 pytest + 端到端 dws 下载待验 |
| v0.6.0 未交付增量 | 2026-09-15 | **工单列表「项目」筛选栏改为只列列表内项目（代码已落，待本机回归）**：主列表「项目」下拉原先取自 `/config/projects/all`（项目表全量），会列出「有项目但没有任何工单」的无效选项。现改为由列表接口按当前列表范围（区域 + 行级范围 + 是否闭环）去重返回 `project_options`——后端新增 `_project_options(db,user,region,scope,closed_only)`（刻意不看 project_id/status/source/priority/person_name/search 二级筛选）+ `schemas.ProjectOption` + `WorkOrderListOut.project_options`，`GET /work-orders` 与 `GET /work-orders/closed/list` 均返回；前端 WorkOrderList/ClosedRecords 项目下拉改读 `list.project_options`、去掉 `getProjectsAll()`，`reload()` 里已选项目不在新结果（如切换区域后）时自动清空并重拉一次。新增 tests/test_project_filter_options.py 3 用例。沙盒 py_compile + vue-tsc 全绿，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-15 | **「我的工单」行级数据范围收口（代码已落，待本机回归）**：修「我的工单页没按自己 id 切到自己的页面」——原页面带「切换人员」下拉、按 person_name 查，普通执行人能切到任意他人/全体工单，且无角色收口。改动 ① 后端新增 `app/services/scope.py`（`visible_scope` / `apply_scope_to_query`）行级范围：admin→全部、区域 PMO（`region_pmos` 配置该 user）→其负责区域 + 本人相关（跨区域被派到的单也含）、其余（executor/approver/readonly）→本人相关（`person_id==me or approver_id==me`）；② `GET /work-orders` 增 `scope=mine` + `user=Depends(require_auth)`，scope=mine 时套行级范围；③ 后端新增 `GET /dashboard/mine`（按行级范围聚合统计卡，返回 scope=all/region/self 供前端标题）；`/dashboard/calendar` 增 `mine=true` 套同样范围（日历与列表一致）；`GET /work-orders/closed/list` 增 `scope=mine`（闭环记录页同样按行级收口）；④ 前端 MyDashboard.vue 去掉「切换人员」下拉，改当前登录人 + `listWorkOrders({scope:'mine'})` + `getMyDashboard()` + `getCalendar(y,m,true)`，`users` 由分页 `getUsers()` 换成全量 `getUsersAll()`（供宽表抽屉选责任人/审批人）；ClosedRecords.vue 列表带 `scope:'mine'`。新增 tests/test_scope.py 8 用例（admin=全量 / executor=仅本人责任人或审批人 / 区域PMO=区域+本人跨区 / 闭环记录 scope=mine / /dashboard/mine 的 self·region 口径）。沙盒 py_compile + vue-tsc -b 全绿，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-14 | **管理员工单操作：详情页编辑基本信息 + 删除「起不了状态」的废单（代码已落，待本机回归）**：① 后端新增 `PATCH /work-orders/{id}/basic`（`require_admin`，`WorkOrderBasicUpdate`：标题/触发原因/行动要求/执行结论/项目/类型/责任人/审批人/优先级/区域/计划开始/截止/完成时间，全部可选、显式 null 清空、region 走 normalize_region、priority 走 normalize_priority，落审计 `update_basic`）——原 `PATCH /work-orders/{id}` 仍 `require_auth` 供派发/回填等流转内改字段，不动；② 后端新增 `DELETE /work-orders/{id}`（`require_admin`，**仅限删「未发起 OA 审批」的废单**——`oa_id` 为空或本地 `OA-` 占位才算未发起；已关联真实钉钉审批单 409 拦截）——先解除非级联外键引用（`data_pool_items.work_order_id`、其它工单的 `triggered_wo_id` 置空）再按依赖顺序显式删子记录（anomaly_occurrences / measure_links / notification_log / escalation_log / attachments / status_log / judgment_degradation_log，不依赖 DB 级联是否真正落库），最后删工单并落审计 `delete`；③ 前端详情页 gate `useUserStore().isAdmin` 显示「✏️ 编辑基本信息」「🗑 删除工单」两按钮——编辑弹窗（标题/项目 t-select 滤组/类型/优先级/区域七选一/责任人审批人 SearchableSelect/三个日期/原因/行动/结论）、删除弹确认（有真实 OA 时点击即 toast 拦截、不进确认，后端 409 兜底）成功后跳列表；④ 新增 tests/test_admin_wo_edit_delete.py 8 用例（admin 编辑生效/非 admin 403/空标题 400/admin 删除后 GET 404/非 admin 删除 403/删除清理池引用与 triggered_wo 引用/真实 OA 删除 409/本地占位 OA 仍可删）。沙盒 py_compile + vue-tsc -b --force 全绿，pytest 待本机 |（最终方案，本机回归 test_notification.py 17 passed）**：发群走企业内部应用机器人 `groupMessages/send`（robotCode=`dinghxbxwotbgkuhxs7d`==AppKey，目标群「[Task] A-双细则 agent」openConversationId=`cidtSHb8Ry9XOmGVBPITSEJ8A==`，OAuth2 token）。三场景：①异常工单抓到平台（每日同步汇总表 generate + 双细则直导）→ `notify_anomaly_new` 按责任人分组合并发群、正文@责任人、找不到@刘冰（`DINGTALK_FALLBACK_USERID`=刘冰 userId）；②措施工单派发 → `notify_measure_dispatch` 首行点「完成人=异常主单责任人」、@措施执行人；③plan 直派发 → `send_dispatch_group`。文案无编号（`_short_title` 去 SXZ 前缀）、行=标题｜优先级｜截止 MM-DD、整行可点链接；合并=按 person_id 分组一人一条。红点@未落地（应用机器人 sampleMarkdown 无 at；要红点再实测 groupMessages 是否带 at 或换自定义 webhook）；遗留=users.dingtalk_id 需统一新格式 userId（@/工作通知认新格式，现 auth union_id / sync 旧格式混存）。触发异步 via tasks.send_dispatch_group_task / send_measure_dispatch_task |
| v0.6.0 未交付增量 | 2026-09-11 | **模板导入工单「没有项目名」修复（代码已落，待本机回归）**：用户报「模板导入的工单没有项目名」。根因：`/import/table` 的 `_import_rows` 用「项目列原文 == `Project.name`」**精确匹配**，而项目名清洗把尾部后缀剥掉后，模板里带后缀的项目名（`国电投别力古台500MW-YH`、`瓜州二期2025`）匹配不到 → `project_id` 落空、列表 `project_name` 为 null，且静默不报。改动 ① 抽出 `app/services/project_names.py::clean_project_name`（单一来源，`oa_ledger._clean_name` 改委托它，消灭 oa_ledger/清洗脚本/imports 三处漂移）；② `imports.py` 建「规范名→项目 + 编码→项目」双索引，`_resolve_project` 先按规范名（可容纳后缀）再按 `PRJ-xxxx` 编码兜底；③ 项目设为必填：填了但解析不到→显式报错并跳过该行、没填→「缺少项目」跳过，不再静默落空。新增 `tests/test_table_import.py` 4 用例（后缀名匹配 / 编码匹配 / 未知项目跳过 / 缺项目跳过）。沙盒 ast/正则自验 PASS，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-11 | **异常指标同步改「准实时」**：从「每天一次」改成「每 5 分钟增量轮询」——config 新增 `anomaly_sync_interval`(秒，默认 300)；`sync_poller.py` 去掉「当日一次」护栏（原 `run_once` 一天只跑一次），改 `run_sync` 每次轮询都跑增量同步（汇总表→数据池→生成工单 + 双细则直导工单），`main.py` 传 `settings.anomaly_sync_interval`；生产 Celery beat `sync-anomaly-daily` 从 `crontab(2:30)` 改 `schedule=300.0`；`.env` `ANOMALY_SYNC_START_DATE` 提前到 09-11（原 09-12 起跑日闸门挡着）+ 加 `ANOMALY_SYNC_INTERVAL=300`。幂等（full=False 按 source_ref/client_request_id 去重），可高频重复调用；重启后端即生效 |
| v0.6.0 未交付增量 | 2026-09-11 | **用户管理两项修复（代码已落，待本机回归）**：① **菜单权限配置不生效**——根因：`savePermissions` 只写前端 store 不落库（刷新即丢）、`/auth/permissions` 后端 hardcode、MainLayout `menuGroups` 对 admin/approver 直接返回全量菜单。改动：后端 `GET /auth/permissions` 改从 `config_definitions`(category=permission/code=menu) 读、新增 `PUT /auth/permissions`(require_admin，校验 roles、actions 缺省保留既有) 持久化；前端抽 `src/menu.ts` 统一菜单定义（MainLayout + 路由守卫共用），菜单过滤全角色走 `canAccessMenu`（admin 恒全量），路由守卫改按菜单权限判断（无权限跳第一个可访问菜单、全无则登出），`savePermissions` 走 PUT 持久化并同步 store、打开页面用已存配置回填复选框（不再默认值覆盖线上）。② **用户列表页布局显示不全**——根因：`.grid2` 1fr/1fr 双卡 + `.card` 固定 `height:calc(100vh-232px)`+`overflow:hidden` 裁剪内容、表格 5 列固定宽 590px 溢出。改动：grid 改 `minmax(0,1fr) minmax(0,440px)`、去固定高度/min-height、断点 900→1200px 提前堆叠成单列，表列宽收窄（合计 530px）+钉钉ID 列 ellipsis。新增 tests/test_auth_enforcement.py 4 用例（默认/非管理员403/保存重读/非法角色400）；沙盒 py_compile + vue-tsc -b 全绿，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-11 | **项目管理「项目名」清洗 + OA 台账名清洗规则修正（代码已落，待本机脚本执行+回归）**：项目名一片脏——老 aitable 同步在「项目简称」末尾堆字母代号/年份（`国电投别力古台500MW-YH`、`协合瓜州二期2025`），`code` 列被写成名称截断，并混入 `不统计 / 华北临时数据 / 东北临时数据 / #N/A` 占位脏名。改动 ① 新增 `scripts/cleanup_project_names_20260911.py`（dry-run 默认 / `--apply` 执行）：垃圾名软删（中文<2 字或命中占位词）→ `clean(name)` 去**尾部**字母代号/年份（截到最后一个中文字符，保留中间「1号/4号」「HS300」）→ 同名去重保留 canonical（`work_orders.project_id`/`person_project_map` 引用改指 canonical）→ 非 `PRJ-####` 编码归一；② `oa_ledger.py::_clean_name` 由「全 strip」改「去尾部 ASCII 后缀」，避免把「丽华鸿蒙4号光伏」洗成「丽华鸿蒙号光伏」（合并 1号/4号 两个场站）、把「瓮安建中HS300风电场」的 HS300 洗掉。快照预演：活动项目 1256 → 667（垃圾软删 4 + 去重软删 585、改名 29、编码归一 432）。交接见 `交接终端prompt-0911新建工单项目下拉与项目名清洗.md`，待本机 dry-run→apply→前端重建→UI 验收 |
| v0.6.0 未交付增量 | 2026-09-11 | **项目下拉可模糊搜索 + 编码排序（新建工单 + 派发弹窗两处）**：用户报「派发工单页面项目名没法选/没法模糊检索 + 下拉一堆乱名」。项目名下拉原来是原生 `<select>`、不能检索，且按拼音散乱、与项目管理页不一致。改动两处都改 `t-select filterable`（支持按名称/`PRJ-xxxx` 编码模糊搜索），下拉项「编码 · 名称」、按编码数字序排（`localeCompare(..., {numeric:true})`，与项目管理页同数据源 `/config/projects/all`）：① `frontend/src/views/WorkOrderDetail.vue` 的「派发→发起OA审批」确认弹窗项目名（`dispatchProjectId`/`projectOptions`）② `frontend/src/views/WorkOrderCreate.vue` 新建工单项目名（默认选中排序后第一个）。「乱名」根因是 projects 表脏，靠上一条清洗脚本落库解决。沙盒 `vue-tsc -b` 全绿；与项目名清洗同批交接（见上一条） |
| v0.6.0 未交付增量 | 2026-09-11 | **工单列表/我的工单「宽表工作台」改造（借故障反馈H5交互理念，代码已落待本机回归）**：列表从「窄列字段网格」升级为一屏看全的宽表行——信息块(编号/来源/优先级/告警/标题/项目/区域)＋时间＋**流程点灯**(复用 `flowProgress`，普通3步/alert五阶段 done/active/warn/todo 点灯)＋触发原因/行动要求＋处理回填(判断/措施闭环进度条/最近异常)＋就地操作列；顶部按钮条加**批量派发/批量重置/导出选中**(行首复选框多选联动)；就地把详情页动作提到列表——一键流转(start_exec/submit_evidence/close/reject/confirm_recovered/dispatch_measures/reset)＋右侧抽屉(派发选责任人审批人、措施工单草稿=待回填根因+措施草稿增删+「确认分析·生成N条」)；新建共用组件 `WorkbenchTable.vue`，WorkOrderList/MyDashboard 接同一组件（我的工单默认落到当前登录人、表数据改拉 rich `listWorkOrders(person_name)`），`selectable` 开关控制行选列；保留全部既有功能(筛选/导入AgentHTML/导出CSV/分页/URL参数/keep-alive/闭环默认归档/钉钉驱动tag)。沙盒 vue-tsc -b + vite build 全绿，本机浏览器点检待做 |
| v0.6.0 未交付增量 | 2026-09-11 | **钉钉OA驱动工单禁止平台手工流转 + 前端提示（代码已落，待本机回归）**：落地「OA 审批是唯一状态源、平台改状态不回写 OA」——原前端列表/详情的手工流转按钮用 `callbackReady`（callback_token+aes_key 都为真）作置灰条件，但 OA 已改 Stream 长连接+10s 轮询兜底，这两个 key 恒空 → 置灰失效，真实 OA 单也能在平台手改状态、与 OA 脱节。改动 ① 后端 `transition_work_order` 对「真实 OA 单（oa_id 非空且非本地 `OA-` 前缀）」拦截 `dispatch/start_exec/submit_evidence/close/reject`，返回 409「该工单已由钉钉OA审批流驱动，请在钉钉OA审批中更改状态」，`reset` 保留兜底；② 前端驱动条件统一为 `hasLiveOA(oa_id)`（去掉 callbackReady），详情页 `oaDriven` + WorkbenchTable `guardOaDriven` 在点击改状态/派发时 toast 同文案（去掉 `:disabled` 静默置灰），WorkbenchTable 删 `callbackReady` prop、WorkOrderList 清掉 `getDingtalkStatus`/`dingtalkStatus`/`callbackReady`；③ tests/test_transition.py 新增 `test_live_oa_blocks_manual_transition`（approving+真实OA 拦派发/驳回、verifying 拦闭环、reset 兜底可走）。沙盒 py_compile + vue-tsc -b 全绿，pytest 待本机 |
| v0.6.0 未交付增量 | 2026-09-11 | **派发工单→钉钉提醒（代码已落，待本机回归）**：责任人离开系统也能收到「新工单待处理」提醒。① `dingtalk.send_work_notification` 重写为旧 oapi `topapi/message/corpconversation/asyncsend_v2`（agent_id+userid_list，卡片带「查看工单」跳 `{FRONTEND_BASE_URL}/work-orders/{id}`）；② 群机器人通道 `robot_mention` 与 `daily_reminder` 改用配置值 `DINGTALK_ROBOT_WEBHOOK`/`DINGTALK_ROBOT_SECRET`（原硬编码空占位）；③ 触发点补齐：手动派发（原有）＋数据池 plan 直派发 `generate_from_pool`＋措施工单B `backfill_work_order`＋`dispatch_measures` 批量派发，统一走 `notification_service.trigger_notify`；config.py 新增 `frontend_base_url`/`dingtalk_robot_webhook`/`dingtalk_robot_secret`，.env 模板+DEPLOYMENT.md 同步补。新增 tests/test_notification.py（策略/消息/asyncsend_v2 payload/群机器人加签/事件映射/池派发触发，mock httpx 不打真网络），沙盒 py_compile + compileall 全过，pytest 待本机。**已配通（2026-09-11 实测）**：`DINGTALK_AGENT_ID=4901376317`（纯数字，与 AppKey 同应用，asyncsend_v2 返回 errcode:0；采坑：ding 开头=应用ID、4724385117=另一应用 agentId，均报 41020 不一致），action_card 用 `markdown`+`btn_json_list`（不是 text/btn_json）；待配：群机器人 webhook/secret、生产 `FRONTEND_BASE_URL`；遗留=userid_list 认新格式 userId，`users.dingtalk_id` 需统一只存新格式 userId |（本机回归 157 passed 2 failed：① `test_build_message_contains_fields` → `build_message` 的 `escalation_level` 无 None 防护已修 ② `test_sync_dual_rule_to_workorders` → 双细则直导 `full=True` 跳过幂等去重撞唯一约束已修（始终预加载 existing）+ `test_daily_sync_gated_before_start_date` 漏 mock `sync_dual_rule_to_workorders` 用真 dws 污染测试库已补 mock；重跑绿 160 passed；最后一轮 msg 改 markdown/btn_json_list+测试断言后待重跑确认） |
| v0.6.0 未交付增量 | 2026-09-11 | **双细则异常子表直导工单（代码已落，待本机回归）**：上游把双细则考核拆成独立子表 `XsYShRm`（30 字段、已是工单形态——SXZ 编号/场站/区域/异常指标(预测准确率·AVC投运率)/实际值/阈值/差距/优先级 P0~P2/严重度 red·yellow·gray/责任人 filterUp user/工单状态「待派单」/验证结论）。`aitable.py` 新增 `sync_dual_rule_to_workorders`：直导 WorkOrder、不经过数据池——`title`=工单标题、`source_code=alert`/`metric_type=dual_rule`、`status=judging`+`alert_phase=confirming`（自带场站回填原因措施→直入 alert 五阶段①「分析确认」）、优先级 P0→P1，`client_request_id=sxz-{SXZ编号}` 幂等去重；`region` 走 normalize_region，`action` 拼「实际值/阈值/差距/严重度/累计异常/备注」摘要，deadline 留空人工补（不臆造）。接 `POST /pool/sync-dual-rule` 手动端点 + 接入 `full_sync`/`run_anomaly_daily_sync` 每日/一键同步。测试：tests/test_cell_utils(+1 user 型 filterUp) + test_anomaly_flow::test_sync_dual_rule_to_workorders（字段映射+幂等），沙盒 py_compile + 纯用例 11 PASS，DB 用例待本机 pytest；**责任人改双细则大类默认（config dual_rule 默认责任人 金惠良→徐林杰，库里已存在行需 PATCH/后台改，system.yaml 只影响全新 seed），表里填报责任人（雷江涛等）降级进 action 摘要「填报责任人 x」** |
| v0.6.0 未交付增量 | 2026-09-11 | **异常指标表上游改表 → 字段映射迁移（代码已落，本机回归绿 146 passed 0 failed）**：上游为「更清晰的工单分类」重构「数据池-异常指标 → 汇总表」结构——原「整改人」字段 `1kgnhr6fqota0ct19aqn6` 变为 **PMO**（singleSelect，王磊阳/陈亮/王爽/任慧敏/谢远操/席鹏程/赵金平），真「整改人」移到新字段 `v6mn5bqeigdt6xmm4a45b`（filterUp 查找引用），新增「原因分类」`xyflaonxfsr57nfnt5df4`（filterUp 多选）；「异常指标」43 选项全重构（自带大类前缀，现 8 大类分类器实测 43/43 全命中）；「区域」选项变区域中心/交付中心/子公司组织名（region_map `_ORG_REGION` 已全覆盖）。改动：`aitable.py` 迁移字段映射——`person_name` 改读整改人新字段，PMO/原因分类落 `raw_data`（键 `pmo`/`reason_category`）；新增纯函数 `services/cell_utils.py`（`cell_to_text` 统一抽取值，含 filterUp 单/多选嵌套），`_cv` 委托之。测试：新增 tests/test_cell_utils.py（10 用例沙盒全 PASS）+ test_anomaly_flow.py::test_sync_anomaly_maps_new_fields（本机 PASS：person_name=整改人、metric_type=power_gen、pmo/reason_category 落 raw）。新结构全量 dump 在 `docs/指引源/异常指标表-新结构-20260911.json` |
| v0.6.0 未交付增量 | 2026-09-10 | **顶栏通知铃铛可交互 + 清理装饰元素**：铃铛原只显示逾期角标、无点击行为，改为 t-popup 点击下拉，展示「逾期工单 + 待办工单」两组消息（数据来自 `/dashboard/stats` 的 overdue_items/todo_items），点条目跳工单详情；移除顶栏「development/production 环境标签」与用户头像（纯装饰、无功能）。沙盒 vue-tsc -b 全绿 |
| v0.6.0 未交付增量 | 2026-09-10 | **alert（监视告警）工单五阶段闭环重设计**：原「生成措施工单并闭环」一步（judging→closed）拆成五阶段——①分析结果确认 ②派发工单 ③已派工单（2/11 跟踪）④指标复核 ⑤指标恢复闭环。数据层新增 `work_orders.alert_phase`、措施工单改 `source_code=measure`（走普通 OA 流转）、`work_order_measure_links` 多对多关联表 + `anomaly_occurrences` 发生记录表（alembic `h001`/`h002`）；**8 异常指标大类**分类器 `services/metric_types.py`（power_gen/curtailment/dual_rule/reliability/info_quality/contract/cost/satisfaction）落 `metric_type`，每类默认责任人 + 分析 Agent 指向进规则配置（`config_definitions category=anomaly_type`，`GET/PATCH /config/anomaly-categories`，默认金惠良，后台 ConfigPage 可改，生成工单责任人解析改为「大类默认>区域PMO>整改人」）；**异常指标每日增量同步轮询**（Celery beat `sync-anomaly-daily` 每天 02:30 + 本地进程内 `sync_poller`，只落新增）；**流转动作** `confirm_analysis`/`dispatch_measures`/`confirm_recovered` + `redispatch`（④指标异常手动补派发回③）+ `reuse`（挂载复用现有措施）/`merge`（合并进同项目同类主单，发生记录迁移）/`similar`（列出同类开着主单）；措施闭环回写主单 2/11 进度、全闭环自动置待复核（手动 close 与钉钉 OA 回传双通道都挂上）。前端：详情页五阶段按钮 + 进度徽标 + 措施列表 + 相似异常卡片 + 补派发弹窗，wo-display 加五阶段/measure 来源。**回归**：新增 tests/test_metric_types(11)+test_anomaly_flow(6)+test_alert_flow(6)+test_alert_merge(5)=28 用例，全量 133 passed 0 failed（本机终端回归绿），vue-tsc exit 0。迁移 h001/h002 本机 alembic upgrade head 单头通过 |
| v0.6.0 未交付增量 | 2026-09-10 | **列表易用性四项 + 项目台账同步**（代码已落，待本机回归 & 本地 Playwright 环境验收，交接见 `交接终端prompt-0910列表易用性与项目台账.md`）① **列宽可拖拽**：工单列表/闭环记录两页 `t-table` 加 `resizable`，标题/触发原因/行动要求等由 `minWidth` 改 `width`（可拖）② **每行补「触发原因/行动要求」内容列** + 列重排（内容列前移、来源后移、操作列 colKey 由 action→op 消歧义）；CSV 导出同步补这两列 ③ **项目筛选改可模糊搜索**：`t-select` 加 `filterable`，数据源由 `getProjects()`（dict 分页 50 条）切 `getProjectsAll()`（全量，模糊匹配不丢项）④ **区域→项目联动**：选区域后项目下拉只显示该区域项目，切区域时清掉跨区域残留的项目；闭环记录后端 `list_closed` 补 `region` 筛选参数 ⑤ **项目台账同步**：数据源=公司 OA（泛微 oa.xh-service.com）网页「运维项目台账」，不是钉钉 AI 表——`services/oa_ledger.py` 新增（Playwright 登录 OA → 台账 customid=97 逐页抓表 → 按「项目状态 ∈ 执行中/待执行」过滤「项目简称」→ upsert projects，凭据走 env `OA_BASE_URL/OA_USERNAME/OA_PASSWORD`，未装 playwright/未配凭据返回明确报错不静默吞），`POST /config/projects/sync-ledger` 端点（require_auth + log_audit），`requirements.txt` 加 `playwright>=1.40`，项目管理页加「从 OA 同步项目」按钮；“执行中/待执行”口径走 `PROJECT_LEDGER_ACTIVE_STATUSES`；**口径**：以 OA 台账为准——只按名称 upsert「执行中/待执行」且名称含中文（纯字母/数字的脏名过滤掉）的项目，同步末尾把不在名单里的既有项目**停用**（is_active=False，非删除、可逆）；工单仍引用这些 project_id 不受影响 |
| v0.6.0 未交付增量 | 2026-09-10 | **外部建单 API（X-API-Key 通道）**：新增 `POST/GET /api/external/work-orders`，供外部系统/Agent 不登录平台建单与查询——鉴权走 `X-API-Key`（env `EXTERNAL_API_KEY`，为空则整接口禁用返回 503）；入参项目/责任人/审批人/类型按「名称或编码」传入、后端解析为内部 id（找不到 404、同名 409，**不自动建实体**）；`client_request_id` 幂等去重（work_orders 新增唯一列 + alembic `g001_add_client_request_id`）；外部建单默认落「待派发(pending)」、不自动发起钉钉审批（由平台内人工派发进 OA）；新增 source `external`（system.yaml seed）；`WorkOrderOut`/`_enrich` 增 `client_request_id` 字段。新增 tests/test_external_api.py 16 用例 + `docs/外部建单接口.md`（含鉴权/字段/解析/幂等/错误码/curl+python 示例），待本机回归 |
| v0.6.0 未交付增量 | 2026-09-10 | **列表状态跨页保留（keep-alive 扩展）**：左侧菜单进入列表页后去详情/其他页再返回，筛选与分页不再重置回初始——`keep-alive` 的 `include` 从仅 `WorkOrderList` 扩到 `DataPool/ClosedRecords/ProjectManage/UserManagement`，各页补 `defineOptions({ name })`；同时 $WorkOrderList$ 加 `watch(route.query)`，缓存态下从看板/来源分布带 query 再次进入时重新应用筛选（干净导航不动已有筛选，避免「返回列表」把已设筛选重置），沙盒 vue-tsc --noEmit 全绿 |
| v0.6.0 未交付增量 | 2026-09-10 | **Excel 导入真正支持 xlsx + 补模板下载**：`/import/table` 原先只 `csv.DictReader` 解析文本（xlsx 二进制走 gbk 解码乱码，虽界面 accept .xlsx/.xls 实为名不副实）；现按文件名/魔数（PK / D0CF11E0）分流，xlsx/xlsm 走 openpyxl 读第一个 sheet，表头中文/英文同义归一（标题/项目/责任人/截止日期/类型/描述/行动要求 ↔ title/project/person/deadline/type/reason/action），导入循环预加载 users/projects/type_kb 并顺序编号；新增 `GET /import/template` 下发「表头+1 行示例」xlsx，前端新建工单 Excel 标签页加「下载模板」按钮；`openpyxl==3.1.5` 补进 requirements（drive_workorder_import 早已依赖却未声明）；旧版 .xls 上传给友好报错引导另存 .xlsx；沙盒 py_compile + 列名归一/CSV/Excel/模板生成纯函数验证全 PASS，待本机 build + 上传实测 |
| v0.6.0 未交付增量 | 2026-09-10 | **告警列进程内兜底（本地实装）**：告警列此前恒空，根因=「标逾期→升级告警」链路挂在 Celery beat（`sla_scan`/`escalation_scan`），本地只起 uvicorn 没人跑；现将 `sla_scan` 抽成纯函数 `run_sla_scan(notify)`（Celery 任务委托之），新增 `app/services/sla_poller.py` 进程内轮询（每 300s 跑一轮 sla+升级扫描，`notify=False` 不发钉钉通知），挂进 `main.py` lifespan（`sla_poller_enabled and not is_prod`），生产仍走 Celery beat 不重复；改动 4 文件 py_compile 通过，待本机 pytest 回归 |
| v0.6.0 未交付增量 | 2026-09-03 | **筛选栏「筛不出东西」修复**：根因=工单列表/闭环记录两页筛选变化时带旧页码查询（翻过页后筛选返回空页，表现"筛不出东西"）；筛选统一走 `applyFilters`（回第1页再查），`reload` 增加「当前页越界（items 空但 total>0）自动回第1页重拉」兜底 + 请求序号丢弃过期响应；静默 `console.error` 改 toast 报错（WorkOrderList/ClosedRecords），DataPool `load` 失败补 toast（原先 unhandled）。其他筛选栏自检：DataPool/UserManagement 本就重置页码 ✓、MyDashboard 纯前端过滤 ✓、SOP/项目管理/操作日志无筛选栏 ✓。沙盒 SFC 编译校验通过，待本机 build+UI 验收 |
| v0.6.0 未交付增量 | 2026-09-03 | **drive 定稿重导修码版（v3）**：v2 本机实测 3 处逻辑 bug+1 下载隐患全修——① 关键词回退整体移除（`_find_by_keywords` 删除，只扫定稿夹，失败/为空明示「已停止，不回退关键词」；回退曾把「我的文件」初稿静默捞进库=本轮 44 条污染根因，属静默失效）② `_resolve_doc_folder` 补 `--space-id`、uuid 从 `result` 层读（顶层 fileId 实测为 None）、失败不再静默吞 ③ `_norm_node` 接受无 extension 的 alidocs file 节点（实测节点 `{nodeId, nodeType:file, name(无后缀), url}`，7 个定稿节点此前全被拒）④ 下载移除不存在的 `doc +download`，`_download_ids` 经 `drive +info --space-id` 换全部候选 id（含数字 dentryId）逐个 `drive download --node`；新增回归 4 条（无 extension 节点/+info result 层/无回退/下载通道），沙盒 drive_import_parse 12+plan_time 13 全 PASS。**数据清场**：库内 `source='plan'` 工单全部来自草稿回退（本轮 44 条：佛山桑得桑20/中建投俊能13/邯郸张西堡11 + 09-02 旧回退约 15 条），重导前盘点→备份 round4→删除，闸门见 `交接终端prompt-0903定稿重导v3.md` |
| v0.6.0 未交付增量 | 2026-09-03 | **工单详情页三项修复**：① `flowProgress` 改三步口径展示（待派发→执行中→已闭环，旁支状态就近归并：approving/dispatched/verifying/overdue→执行中、rejected→待派发标 warn；原 liveOA 折叠分支撤换；用户二次确认要三步） ② 「🔄 同步 OA」手动按钮去 `hasLiveOA` 条件恢复常驻，`success:false`（未发起 OA 等）改弹 `toast.warning` 不假报成功 ③ 详情页 15s 轮询：`pollActive`（有真实 OA 且非 closed/rejected）时先 `syncOaWorkOrder` 让后端拉钉钉状态/附件再静默刷新 wo/logs/attachments（不打断编辑态，`document.hidden` 跳过，`onUnmounted` 清理），审批卡显示轮询提示；沙盒 vue-tsc -b 通过 + 逻辑桩 ALL PASS，待本机 build/UI 验收 |
| v0.6.0 未交付增量 | 2026-09-03 | **主列表已闭环默认归档**：后端 `GET /work-orders` 增 `include_closed`（默认 false 排除 closed；显式 `status=closed` 视为查归档仍返回），`/work-orders/closed/list` 不变；前端主列表状态下拉去掉「已闭环」、带 `status=closed` 旧链接重定向 `/closed`、看板「已闭环」统计卡改跳闭环记录页；新增 tests/test_list_archive.py 5 用例（沙盒无 pytest/PG 未跑，待本机回归） |
| v0.6.0 未交付增量 | 2026-09-03 | ① 项目编码统一 `PRJ-####` 系统自动分配、创建后不可改（手工新增/aitable 同步/seed 三入口收编），alembic `f001_normalize_project_codes` 一次性归一历史杂码（名称截断码、TL-YX 旧种子码）② 用户管理页：`/auth/users` 支持 q 搜索（姓名/钉钉ID 服务端过滤）+ 搜索框 + 双卡等高对齐 ③ 操作日志接通：读接口字段修正（target_type/actor_id），项目 CRUD/用户角色与启禁/工单创建与流转写审计，前端操作与对象中文化 ④ 新增回归 tests/test_codes_search_audit.py，待本机 pytest ⑤ **登录收编**：钉钉 OAuth 为唯一入口——不自动建号、仅已有启用用户可绑定、`LOGIN_ADMIN_ONLY=true` 时仅管理员放行（李沛东）；姓名登录/开发登录默认关闭（`NAME_LOGIN_ENABLED`/`DEV_LOGIN_ENABLED` 开关，生产恒关）；OAuth 回跳改前端登录页（`DINGTALK_LOGIN_REDIRECT_URI`，兼容 authCode/code 参数）；seed admin 用户改名李沛东；前置：钉钉开发者后台登记回调域名（疑似要求 HTTPS，IP/http 能否录入待验）。同日恢复本机开发登录（`DEV_LOGIN_ENABLED=true` + 前端 VITE_DEV_LOGIN，见「局域网试用开放」节）⑥ **drive 定稿导入**：sync-drive 只扫钉盘定稿文件夹（spaceId=26687726819/fileId=230610076351，其他位置初稿不扫；定稿夹是 alidocs 节点，列举走 `doc +list`、数字 dentryId 经 `+info` 换 uuid，下载 drive/doc 双通道尝试），「计划时间/时间窗口」经新模块 `app/services/plan_time.py` 解析出计划开始+截止，无「工单载体」列文件按非EAM 整表导入（业务拍板），重导入补空不补错；新增回归 21 用例（沙盒逐条 PASS），本机重跑见 `交接终端prompt-0903定稿重导.md` ⑦ **测试闸门修复（09-03 晚）**：AuditLog 模型补 `created_at` 列（物理表 0001 迁移本就有，模型漏声明致 /audit-logs 序列化 AttributeError；无需新迁移）；tests/conftest.py 加登录开关归位夹具（本机 .env 应急开关不再泄漏进测试）；门禁旧报告中的「姓名登录 403」系旧版测试残留口径，重写版已落盘；同日 drive 定稿导入通道修正：定稿文件夹为 alidocs 节点，drive 通道列举必 RESOURCE_NOT_FOUND/accessDenied，`_walk_folder` 改走 `doc +list --folder`（数字 dentryId 经 `drive +info` 自动换 uuid），下载 `drive download`/`doc +download` 逐组合落盘为准；套件口径 39→80 用例，待本机全量回归 |
| v0.6.0 | 2026-09-02 | 上线前加固：① 后端全路由强制鉴权（豁免仅登录/health/OA回调/bot）② 姓名登录（无密码，用户拍板；钉钉 OAuth 为主入口）③ AUTO_SEED 生产硬拦截演示数据 ④ Alembic 双头修复（e001_merge_heads）⑤ 数据源容器化：dws 内置镜像+凭证包 Secret 注入，钉盘年度计划非EAM行/AI表格异常两来源均可手动触发（/pool/sync-drive、/pool/sync-aitable、/pool/dws-status）⑥ Dockerfile 构建上下文修正 ⑦ 判断 Agent v1 停用（降级日志兜底）⑧ 修复项目映射入库撞码 500、drive download 假解析报错 ⑨ 测试重写对齐判断流契约，本机回归 39 passed |
| v0.5.0 | 2026-08 | 判断 Agent + 措施工单闭环 + agent 复盘导入（commit 5482e92） |
| 更早 | — | 见 git 历史（831a5c4 等） |

## 局域网试用开放（2026-09-03 验证通过）

- **固定入口：同网段访问 `http://10.10.147.200:5173`**（en0 已改手动静态配置：255.255.252.0 / 网关 10.10.144.1，持久化，重启不丢；旧地址 10.10.147.84 已作废）。
- 后端 8000 / 前端 5173 均 `0.0.0.0` 监听，macOS 防火墙当前关闭。
- 前提：本机保持开机且连着当前 WiFi；前后端进程在跑。
- OA 同步免公网：事件走钉钉 Stream 长连接 + 10s 轮询兜底（均出站），附件经 oapi 主动拉取；钉钉后台无需配 HTTP 回调地址。
- 登录：2026-09-03 收编为钉钉 OAuth 且限管理员（李沛东）；但钉钉回调域名未落地（控制台疑似不收 http/裸 IP），**本机已恢复开发登录**：`backend/.env` `DEV_LOGIN_ENABLED=true` + `frontend/.env.development` `VITE_DEV_LOGIN=true`（该文件已加 .gitignore 不入库），登录页可选「李沛东 (admin)」走开发环境登录。姓名登录仍关（应急开关 `NAME_LOGIN_ENABLED=true`）。生产恒关不受影响（`is_prod` 拦截）。待钉钉回调域名打通后再收回开发登录。若开防火墙需放行 node(vite)/python(uvicorn) 传入连接。
- 风险备忘：.200 为手选静态地址，若公司 DHCP 池覆盖高位理论上有撞车可能；真撞了回滚 `sudo networksetup -setdhcp Wi-Fi`，根本解是路由器 DHCP 保留（绑 MAC aa:34:1e:95:66:99）。

## 待办（非代码，上线前置）

1. 本机无 Docker：镜像构建实测需装 Docker Desktop/Colima，或在云上首次构建验证
2. 钉钉后台：应用事件订阅（bpms_instance_change/bpms_task_change）+ 审批数据读取权限；不配则由 10s OA 轮询兜底
3. dws 凭证包（k8s Secret `wo-dws-auth`）refresh token 约 30 天轮换，流程见 `docs/DEPLOYMENT.md` 3.1
4. 判断 Agent：v1 未部署（`JUDGMENT_ENABLED=false`），外部镜像交付后取消 k8s.yaml 注释再接入

## 数据侧跟进（不阻塞上线）

- 钉盘文件「工单版-运营计划」×2 文件名无项目名 → 匹配不到项目，建议项目侧重命名带项目简称
- 无「工单载体」列的年度计划表（北京泽釜海门储能、夏津温辛汇安等）：2026-09-03 拍板按非EAM 整表导入（不再跳过），EAM 重叠靠 项目+标题 去重与人工抽查兜底
- 计划开始/截止空列：根因为时间列写法多样+无载体列表整表跳过；已修（plan_time 解析+补空回填），存量补数待本机重跑（v3 重导一并完成，见 `交接终端prompt-0903定稿重导v3.md`）
- **草稿污染清场（2026-09-03）**：定稿重导 v2 实测失败后走了关键词回退，误入 44 条初稿工单（佛山桑得桑储能20/中建投俊能13/邯郸张西堡11）+ 9 条补空；叠加 09-02 旧回退约 15 条，库内 `source='plan'` 工单全为草稿来源（定稿表从未成功入库）。处置：v3 重导前盘点→备份 round4→按 `source_code='plan'` 全量删除（删前核对行数），重导后库内 plan 工单只来自 7 个定稿表
- **异常指标数据口径（2026-09-10）**：异常指标表只进「7 月及以后」（`_keep_month`，1-6 月/空/`25年分析` 未标注一律 skip）；已用 `scripts/cleanup_anomaly_20260910.py` 清掉此前灌入的异常主单 2619 条 + 异常数据池 4924 条，重置回种子/手动/plan 共 134 条；7 月+ 的 340 条在数据池 pending，待每日定时同步生成（起跑日 `ANOMALY_SYNC_START_DATE=2026-09-12`，之前不自动生成）
- **历史 alert 主单五阶段归位（2026-09-11）**：五阶段上线后历史 alert 主单 `alert_phase` 为空、前端仍按三步展示且不能手写措施。已用 `scripts/backfill_alert_phase_20260911.py` 回填：9 条 `closed`→`recovered`（历史闭环单终态「已恢复」）；无历史 `judging` 主单；5 条 `pending`（RW-2026-0089~0093）待回填、规则规定不动；9 条 `overdue` 均为旧流程误标成 alert 的「措施单B」（被 `triggered_wo_id` 引用）已正确排除。只手写 `alert_phase`、不碰 `status`，每条补 StatusLog 留痕，幂等可重复跑；回退可按 note 前缀「历史回填·阶段归位」定位置回 NULL
- **alert 手动回填表单恢复 + 回填/措施合一（2026-09-11）**：五阶段改造误删了 `WorkOrderDetail.vue` 里 pending alert 的手动回填表单（只留「判断Agent 导出/导入」离线桥），导致 pending 告警单无 Agent 结果就无法回填、进不了「分析确认」、措施草稿与五阶段按钮一概不出现（用户实测「走不通、没法测」）。已补回并进一步合并：pending 阶段直接「根因分析 + 措施工单列表（可分条）」一步填完，`submitBackfill` 存措施草稿(`triggered_wo_tasks`)+回填根因进入 confirming；confirming 阶段同一个编辑器预填、纯复核，头部「确认分析结果」一键生成工单；去掉自由文本「应对措施」字段（避免同一事填两遍）。后端 `backfill` 接口本就 pending→judging 且置 `alert_phase=confirming`、不依赖 Agent，前端只缺入口。vue-tsc -b 通过，待用户本机点一条 pending 告警单走通全链验收
- **plan 工单标题（2026-09-10）**：钉盘「工单版」导入不再把原编号 `WO-xxx` 拼进标题（保留在时间线备注），已导入 71 条标题已用 SQL 去前缀

## 技术栈

前端 Vue3+Vite+TS+TDesign · 后端 FastAPI+SQLAlchemy 2.0 · PostgreSQL 15 · Tair/Redis · OSS · ACK+ALB · dws CLI（钉盘/AI表格数据通道）

## 目录结构

```
wo-closed-loop/
├── backend/   # FastAPI + Alembic + pytest（tests/ 133 用例）
├── frontend/  # Vue3 + TDesign
├── config/    docker/    # k8s.yaml、Dockerfile、entrypoint
├── docs/      # DEPLOYMENT.md 部署 Runbook、reliability-agent 契约/黄金样本
├── scripts/   docker-compose.yml  Makefile
├── backend/.env.production.example   # 生产配置模板（真实 .env 不入库不入包）
└── *.html     # 工单闭环方案/面板对比等设计稿
```
