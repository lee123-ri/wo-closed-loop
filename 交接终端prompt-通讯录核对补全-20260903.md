# 交接 prompt：钉钉通讯录 vs 系统用户表 核对与补全

你在用户（李沛东）本机 Mac 上执行。目标：把公司钉钉通讯录的真实人员，跟「工单管理平台」数据库里的用户表核对一遍，找出漏导的人并补全（同时补上部门字段）。这是本机才能做的操作，交给终端 Claude 执行，用户只验收结果。

## 背景现状（已从 15:15 数据库备份 backup_wo_20260903_round2.sql 核对，供你比对参考）

系统 users 表当前约 **1499 个启用用户**：
- role 分布：admin 1、approver 13、executor 1485，全部 is_active=t。
- **4 人缺钉钉 ID**：明南辉、陈立超、admin（=李沛东那行，本机库尚未应用 seed 改名）、郭宝记。
- **几乎全员缺部门**（1496/1499 的 department 为空），仅金惠良/陈亮（交付管理部）、贾兴威（管理层）3 人有部门。
- 存在同名用户（刘洋×3、陈健×2 等）——钉钉 OAuth 登录不受影响，但姓名登录会因同名报 409。

## 项目位置与运行前提

- 仓库：`/Users/lee/Documents/work/wo-closed-loop`
- 后端：`cd backend && . .venv/bin/activate`（Python FastAPI + SQLAlchemy 2.0），数据库 PostgreSQL 本机原生跑（非 docker）。
- 通讯录同步脚本：`scripts/sync_dingtalk_users.py`（手动脚本，遍历钉钉组织架构全员 upsert 入库，新用户默认 role=executor、is_active=true，写入 dingtalk_id/department/department_id）。
- 该脚本依赖 `dws` CLI 且已登录认证。**注意：dws 凭证包 `~/Documents/work/dws-auth.tar.gz` 已知过期**，若 dws 调不通先重新登录。

## 执行步骤

1. **确认 dws 可用并已认证**
   - `dws --help`（或 `dws auth status` / `dws status`，以实际 help 为准）看是否登录。
   - 若未登录：`dws login` 完成，再 `DWS_DISABLE_KEYCHAIN=1 dws auth export` 导出新凭证包（可选，供容器/沙盒用）。
   - 若连 `dws` 命令都不存在，参考仓库 `desktop-dws-restore` 相关说明安装 `dingtalk-workspace-cli` 并 import 凭证。

2. **先核对 dws 通讯录子命令的实际名字**
   - 脚本里用的是 `dws contact +list-sub-depts --dept <id>` 和 `dws contact +list-dept-members --depts <id>`，但 dws 真实旗标名可能不同。先 `dws contact --help` 确认「列子部门」「列部门成员」两个子命令与旗标名，跑通后再继续。

3. **拉取通讯录全员并统计**
   - 用确认后的命令，从根部门(1)开始递归所有子部门，拼出「通讯录全部成员」清单，统计**去重后人数**。记录这个数 N。

4. **与系统用户表对比（这是核心，先看清楚再补）**
   - 连本机 PG（后端 `.env` 里 DATABASE_URL）执行：
     - 系统总用户数：`SELECT count(*) FROM users WHERE is_active;`
     - 缺钉钉 ID 的用户：`SELECT id,name,role FROM users WHERE dingtalk_id IS NULL;`
     - 有部门的用户数：`SELECT count(*) FROM users WHERE department IS NOT NULL;`
   - 把「通讯录清单（userId→姓名→部门）」与「users 表（dingtalk_id→姓名）」做双向 diff：
     - 通讯录有、系统无 → 漏导（要补）；
     - 系统有、通讯录无 → 可能是离职/停用，先只报告不要自动禁用。

5. **补全**
   - 首选直接运行 `python3 scripts/sync_dingtalk_users.py`（脚本幂等：按 dingtalk_id 或 name 匹配，缺则新建、有则更新部门和 ID）。观察打印的「新建/更新/跳过/系统总用户」。
   - 跑完再核对：系统总用户数是否 ≥ 通讯录人数 N；若仍明显少于 N，说明有部门没遍历到（dws 权限/部门边界问题），把缺的部门报出来，不要硬塞。
   - 确认那 4 个缺钉钉 ID 的人（明南辉/陈立超/admin/郭宝记）是否被正确匹配补上；admin 那行保持 role=admin 别覆盖。

## 必须回传给用户的结果

按下面格式回传，用户据此验收（不达标不要停）：

1. 通讯录去重后总人数：___
2. 同步前系统启用用户数：___
3. 同步脚本打印的 新建/更新/跳过 各多少：___/___/___
4. 同步后系统启用用户总数：___（应 ≥ 通讯录人数）
5. 仍漏导的人员/部门（若有，列出来）：___
6. 4 个缺钉钉 ID 的人是否已补上及结果：___
7. 部门字段补上的用户数：___

## 红线

- 只增补、不删除、不自动禁用任何现有用户；离职人员只报告。
- 不要改动 admin（李沛东）的 role。
- 不要动数据库 schema、不要跑 alembic。
- 若 dws 拉不到通讯录（凭证/权限问题），停下来说明卡点，不要臆造人员数据。