# 工单闭环项目约束

继承 `~/Documents/work/AGENTS.md` 和全局规则。本项目涉及业务数据与 OA 集成，禁止将凭据、回调密钥或生产数据提交到仓库。

## 业务边界

- 只将标为“非 EAM”的项目导入工单；EAM 项进入 EAM/设备预警流程，不能混导。
- 区域字段只使用七大区枚举，不填省份或空字符串。数据库、API 和前端选择器必须保持一致。
- 可靠性 Agent 的输入转为待确认草稿，不自动派发。异常指标判断需支持“不发现场关闭”，并留下关闭原因和可追溯记录。
- OA 审批拒绝是终态；退回修改应采用平台 `returned` 状态和重新发起的新 OA 实例，不能假设原实例可编辑。

## 变更与验证

- Alembic 迁移编号必须线性且唯一，合并分支前检查迁移链，避免同编号分叉。
- 测试必须使用独立数据库，禁止连接主库或让测试重置生产序列；导入显式 ID 后同步 PostgreSQL 序列。
- 真机 OA 联调前确认凭据、模板、回调地址、迁移和用户 ID 映射均已就绪。缺任一外部条件时标注“待联调”，不可称生产已验证。
- 公网暴露前先落实 API 级鉴权或网关访问控制；前端登录页不能代替服务端鉴权。
