# 工单建设情况介绍（回复 jiaxw 版）

> 编制 2026-09-10 ｜ 依据：仓库实际代码（backend/ FastAPI + models）与 `docs/工作逻辑说明_业务对照版.md`、`总纲.md` 核实 ｜ 当前版本 v0.6.0
> 口径约定：**绿＝已实现且可用**；**黄＝代码已就绪但未上线/未全量回归**；**红＝未支持/未打通**。以下逐条对应你的四个问题，不合并、不充数。

## 一句话结论

平台代码已到 v0.6.0，主干功能基本成形、85 条回归用例，但**尚未正式上线 ACK**——目前只在李沛东本机局域网试用（开发登录，仅管理员可进），数据也只覆盖「刘冰的 HS500 重点监控 8 个项目」且未齐。所以给 SOP 用之前，请把「代码写完」与「线上稳定运行」区分看待。

---

## 一、已上线 / 在建 / 尚未支持 + 使用范围

**已实现（代码就绪）**：工单 CRUD、状态流转、审批流引擎、数据池（来源→生成）、SLA 扫描与升级、通知引擎、权限鉴权（JWT + 角色）、操作日志审计、项目/用户/SOP 知识库管理、钉钉 OAuth 登录、钉盘与 AI 表格数据源同步。

**在建 / 未上线（代码已入、待本机回归或待部署）**：项目编码统一、用户管理搜索、操作日志接通、登录收编为钉钉 OAuth 且仅管理员、drive 定稿导入、主列表闭环默认归档、详情页审批三步展示与 15s 轮询同步、告警列进程内兜底（本地）。这些都属于「写了没上线/没回归完」。

**尚未支持（明确没做，GAP 分析已列）**：看板视图、批量操作、工单模板、日历视图、报表导出（PDF/Excel，仅部分）、移动端、自助门户、自动化规则。

**当前使用范围**：本机局域网（`http://10.10.147.200:5173`）；数据只有两条来源——钉盘定稿年度计划（非 EAM 行）+ 钉钉 AI 表格异常指标，且当前异常类 7/8、年度计划定稿 0/8 真正入库（含草稿污染待清场重导）。范围之外不要假设有数据。

---

## 二、能力逐条对照（每条对应回复）

| 你问的要素 | 状态 | 说明 |
|---|---|---|
| 责任人 | 🟢 支持 | 每单有责任人（+审批人），按人可查「我的工单」 |
| 期限 | 🟢 支持 | 计划开始时间 + 截止时间两个日期字段；逾期回算逾期天数 |
| 提醒升级 | 🟡 部分 | SLA 提前预警 + 逾期标红 + 升级阶梯（逾期 1/3/7 天升 L1/L2/L3）都有，但**通知通道只有钉钉工作通知真正生效**，群机器人 webhook 未配、短信是 mock |
| 验收 | 🟢 支持 | 待验收→已闭环，由审批人在钉钉 OA 确认闭环 |
| 效果观察 | 🔴 未支持 | 无独立「观察期/回访/复评」环节，闭环只记结论与完成日期；SOP 里这一步系统顶不住，需补建 |
| 关闭 | 🟢 支持 | 闭环后默认归档到闭环记录页 |
| 重开 | 🔴 未支持 | closed 是终态、无 reopen；仅有一个测试用 reset 回到待派发，非业务重开 |
| 一问题多措施 | 🟢 支持 | 判断流程支持「1 条宿主工单 + N 条措施工单」，每条措施可带各自责任人/期限/优先级 |

---

## 三、接口与钉钉打通（每条对应回复）

**可用接口**：标准 REST（FastAPI + JWT）。没有对外公开 API/SDK，第三方集成入口只有钉钉 OA 回调和群机器人 webhook。主要端点：工单 `/work-orders`（列表/详情/创建/`transition` 状态流转/`closed` 归档）、数据池 `/pool`（`sync-drive`/`sync-aitable`/`generate`）、导入 `/imports`（听记解析、Agent 批次导入）、`/dingtalk/oa/*`（状态查询/同步）、`/auth`、`/bot`、`/dashboard`、`/config`、`/admin`。

| 你问的能力 | 状态 | 说明 |
|---|---|---|
| 创建草稿 | 🟡 部分 | 工单主表没有 draft 态（最小是「待派发」），但**措施工单有草稿**——Agent 导入/判断环节先把措施存成草稿列表、不上工单列表 |
| 人工确认派发 | 🟢 支持 | 监视告警主单走**五阶段闭环**：分析结果确认（人工确认/改措施）→ 派发工单（补责任人/审批人/类型/时间下发）→ 已派工单（2/11 跟踪）→ 指标复核 → 指标恢复闭环；普通建单由人工点「派发」发起 OA |
| 查询状态 | 🟢 支持 | 工单列表/详情/归档、钉钉 OA 实例状态查询、10s 轮询兜底同步 |
| 回写凭据 | 🟡 部分 | OA 佐证附件回写工单（OSS）✓、回填原因/措施/时间写回数据池 ✓；但**钉钉审批表单是快照，平台无法把状态/字段写回表单**——钉钉才是状态源 |

**与钉钉三方打通进展：**

| 通道 | 状态 | 说明 |
|---|---|---|
| 指标数据 | 🟢 已打通 | dws 读钉钉 AI 表格异常指标表 + 映射表（回填项目/责任人/区域），手动触发同步 |
| 消息 | 🟡 部分 | 输出侧：钉钉工作通知（JSON 卡片）已通；群机器人 @ 建单已实现但 webhook 未配；**群消息作为工单来源「暂缓」** |
| 听记 | 🔴 基本未通 | 后端有「粘贴听记文本→LLM 解析出工单」接口，但前端隐藏、且**未与钉钉听记自动拉取打通**，只是手动贴文本 |

---

## 四、AI 能力 / 仍需人工环节 / 卡点 / 后续计划

**已有 AI 能力（就两处）**：① 听记文本结构化解析（通义千问，后端接口）；② 判断 Agent（异常回填后自动给措施建议），但 **v1 未部署（JUDGMENT_ENABLED=false）**，目前降级为「按人工输入直建措施单」或「导出 JSON→技术团队 Agent→导回」的离线桥接。

**仍需人工的环节（正是可自动化的重点）**：数据源同步（手动点）、派发前必填字段补齐、回填原因/措施（责任人手填）、措施单类型/责任人选定（PMO 手选）、验收闭环（审批人手点）。

**主要卡点**：① 本机无 Docker，镜像构建/ACK 部署未验证；② 钉钉回调域名/事件订阅未落地，push 收不到、靠 10s 轮询兜底；③ 判断 Agent 未上线；④ 数据源侧配合（钉盘文件名缺项目名、缺「工单载体」列）；⑤ SLA/升级数值系统自定、无官方指引源，待业务确认。

**后续计划**：先打通 ACK 部署 + 钉钉事件订阅、判断 Agent 上云，再按 GAP 补看板/批量/模板/报表；数据同步是否定时、SLA 口径、效果观察环节均列在待业务确认清单。

---

## 五、可共享的现成文档（已脱敏）

- `docs/工作逻辑说明_业务对照版.md` —— 业务侧对照说明（最适合 jiaxw 读）
- `总纲.md` —— 状态与版本全貌
- `docs/规则台账-待业务确认.md` —— SLA/优先级待确认口径，正好和 SOP 整改对应
- `docs/GAP_ANALYSIS.md` —— 差距与补强清单（避免重复建设）

---

## 附：业务问题复盘会 SOP × 平台能力对照表（能顶 / 顶不住）

| SOP 跟踪整改环节 | 平台现状 | 能否直接用 | 缺口 / 需补建 |
|---|---|---|---|
| 问题登记（来一件记一件） | 有（手动建单 / 数据池 / 群机器人 @） | 🟢 能顶 | 群机器人 webhook 未配，暂用手动/数据池 |
| 责任到人 | 有（person_id，单一责任人） | 🟢 能顶 | — |
| 期限 | 有（计划开始 + 截止） | 🟢 能顶 | — |
| 分级 | 有（P1/P2/P3，来源兜底） | 🟢 能顶 | SLA 数值无官方源，需确认 |
| 提醒 | 有（SLA 提前预警） | 🟡 部分 | 只走工作通知，群机器人/短信未通 |
| 升级 | 有（逾期 1/3/7 天逐级） | 🟡 部分 | 同上，通道未全量 |
| 进展更新 | 有（状态流转 + 操作日志） | 🟢 能顶 | — |
| 回填原因/措施 | 有（backfill 字段） | 🟢 能顶 | 纯人工填写 |
| 验收 / 闭环 | 有（审批人确认闭环） | 🟢 能顶 | — |
| 效果观察 / 回访 | 无独立环节 | 🔴 顶不住 | 需新建：观察期、复评、回访提醒 |
| 关闭归档 | 有（闭环默认归档） | 🟢 能顶 | — |
| 重开 | 无（closed 终态） | 🔴 顶不住 | 需新建 reopen 动作 |
| 一问题多措施 | 有（N 条措施草稿→生成） | 🟢 能顶 | — |
| 凭据留存 | 有（附件回写 OSS） | 🟡 部分 | 仅 OA 附件，自动抓证不全 |
| 闭环后统计复盘 | 弱（看板部分导出） | 🟡 部分 | 报表导出(PDF/Excel)未做全 |

> 说明：把 SOP 里「效果观察」「重开」两步定义为需要平台侧补建，其余多为「已有但未上线/通道未全量」，不是重复建设的方向。