# 终端交接 prompt —— 0903 筛选栏修复本机验收

> 用法：把本文件内容整段交给本机终端 Claude 执行；桌面端 Claude 只做代码修改与最终验收。
> 项目目录：~/Documents/work/wo-closed-loop（当前工作树，勿切分支）

## 背景（一句话）

工单列表/闭环记录「筛选栏没法筛出东西」根因已定位并修复：筛选变化时带旧页码查询，翻过页后筛选返回空页。修复落在 `frontend/src/views/WorkOrderList.vue`、`ClosedRecords.vue`（`applyFilters` 回第1页 + 越界自动回拉 + 过期响应丢弃 + toast 报错）与 `DataPool.vue`（load 失败补 toast）。需本机 UI 验收 + 全量测试回归。

## 验收步骤

1. 启动：`bash run-local.sh`（后端 8000 + 前端 5173；如已在跑，vite 会热更，确认终端无编译报错）。
2. 登录：开发登录（李沛东 admin）。
3. 工单列表 `/work-orders` 筛选栏逐项验收（种子约 30+ 工单，够 2 页）：
   - **先翻到第 2 页**，再依次使用：标题搜索（输入"风机"或任一可见标题关键词）、项目、区域、来源、状态、优先级、责任人搜索 —— 每项选中后表格必须立刻回到第 1 页并显示匹配结果（修复前：第 2 页筛选=空表）。每项用完清除再测下一项。
   - 第 1 页直接筛选同样正常；组合两个筛选正常；「重置」恢复全量。
   - 筛选后分页器 total 与结果一致；筛选态下翻页正常。
   - 断网/停后端再点筛选：应弹 toast「工单列表加载失败：…」而不是静默无反应（验完恢复后端）。
4. 闭环记录 `/closed`：项目/来源两个筛选同样从第 2 页起测，行为同上。
5. 其他筛选栏快检（自检确认无同类 bug，走一遍即可）：数据池 `/pool` 类型/状态筛选（应回第1页）、用户管理搜索（应回第1页）、我的工单切人、SOP/项目管理/操作日志无筛选栏。
6. 测试回归：`cd backend && .venv/bin/python -m pytest tests -q`（口径 85 用例，应全绿；若 conftest 需 PG，按 run-local-tests 技能用 docker compose 的 db）。
7. 前端构建闸门：`cd frontend && npm run build`（vue-tsc -b 须过）。

## 回传格式

- 每步 PASS/FAIL；FAIL 附截图+控制台/网络面板摘录。
- pytest 尾行（N passed）与 build 结果。
- 若第 3 步任一筛选仍"筛不出东西"：抓该次请求的 URL+响应 JSON 一并回传（说明根因不在页码，需桌面端再查）。
