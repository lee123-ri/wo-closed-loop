# 交接终端：「Excel 导入加确认录入」+「删除工单 405」本机回归与重启

**背景**：王宁（风机主管，系统里 role=admin）反馈两个问题：
1. Excel 导入「直接录入了」，想要先确认再录；误导入后想删又删不掉。
2. 删除工单报「删除失败：method not allowed」。

**结论已定位（沙盒核实）**：
- 删除 405 不是代码逻辑错，是**运行中的后端还是旧代码**——`backend/app/api/workorders.py` 里的 `DELETE /work-orders/{wo_id}` 路由是 09-14 的工作区改动（未 git 提交），本机后端进程当时加载的旧代码没有这个路由，所以 DELETE 请求落到「有 GET/PATCH 但无 DELETE」的路径上，返回 405 Method Not Allowed。
- 删除功能本身已写全（admin 可删、清理子表、真实 OA 拦截 409、非 admin 403），本轮**不改删除逻辑，只验证它能跑**。
- 导入确认是**本轮新增**：后端加了 `/import/table/preview`（解析预览不落库）+ `/import/table/confirm`（确认落库），前端「新建工单 → 从 Excel 导入」改成「上传 → 预览勾选 → 确认录入」。

## 改了哪些文件（沙盒已 py_compile + vue-tsc -b 全绿）

- `backend/app/api/imports.py`：抽出 `_resolve_deadline`（预览/落库同口径）；新增 `_preview_rows`（逐行 ok/error，项目必填未匹配标红）；新增 `POST /import/table/preview`、`POST /import/table/confirm`（收 `{rows: [raw...]}`）。
- `backend/tests/test_table_import.py`：新增 2 用例（preview 不落库、confirm 落库勾选行）。
- `frontend/src/api/imports.ts`：新增 `importTablePreview` / `importTableConfirm` + 预览类型。
- `frontend/src/views/WorkOrderCreate.vue`：Excel 导入改成「预览表格（TDesign t-table 勾选）→ 确认录入 N 条」，默认勾选所有 ok 行，问题行（缺标题/项目未匹配）标红且默认不勾选。

## 本机回归步骤

### 0. 关键：重启后端（否则 DELETE 仍是 405）

```bash
cd ~/Documents/work/wo-closed-loop
# 杀掉旧后端，确认无残留
pkill -f "uvicorn app.main:app" 2>/dev/null || true
sleep 1
# 重新起后端（--reload；数据库依赖若没起先 docker compose up -d db redis）
bash run-local.sh
```

后端起来后，确认 DELETE 路由已加载（二选一）：
```bash
curl -s -X OPTIONS http://localhost:8000/api/work-orders/1 -i | grep -i allow
# 期望看到含 DELETE 的 Allow 行
```
或打开 http://localhost:8000/docs 找 `DELETE /api/work-orders/{wo_id}`。

### 1. 后端测试（先清缓存）

```bash
cd ~/Documents/work/wo-closed-loop/backend
find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
rm -rf .pytest_cache
.venv/bin/python -m pytest tests/test_table_import.py tests/test_admin_wo_edit_delete.py -q
```
预期：这两文件全绿（table_import 老 4 例 + 新 2 例；admin_wo_edit_delete 8 例）。

### 2. 全量回归

```bash
.venv/bin/python -m pytest -q
```
预期：0 failed（在既有基数上 +2）。

### 3. 前端 build（类型闸门，本机确认）

```bash
cd ~/Documents/work/wo-closed-loop/frontend
npm run build
```
预期：退出码 0。

### 4. UI 点检（开发登录选「李沛东 (admin)」）

- **删除**：找一张未派发的 manual 工单进详情 → 点「🗑 删除工单」→ 应弹确认、确认后跳列表、该单消失（**不再是 method not allowed**）。
  - 找一张**已发起真实 OA** 的单 → 点删除 → 应被 toast 拦截（不弹确认）。
  - 换 admin 直接 `curl -X DELETE http://localhost:8000/api/work-orders/{id}` 也应 200；非 admin 应 403。
- **导入确认**：「新建工单 → 从 Excel 导入」→ 上传 xlsx/csv → 应出现**预览表格**（不是直接录入），行数/可录入/有问题数对得上；问题行默认不勾选；勾选几行点「确认录入」→ 只落库勾选的行，结果提示条数正确。

## 回报

① 后端重启后 `/docs` 是否出现 DELETE 路由（或 curl OPTIONS 的 Allow 含 DELETE）；② 两文件 pytest 结果；③ 全量 pytest 尾部（passed/failed）；④ `npm run build` 退出码；⑤ UI 点检删除 + 导入确认的通过/异常（异常截图/console 报错原样报）。

**约束**：只跑测试与点检，不改代码。任何一步报错原样回报，别自行改代码；需要改代码时回传给我。